#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int h, x;
        cin >> x >> h;
        cout << (x >= h ? "YES" : "NO") << endl;
    }
    return 0;
}